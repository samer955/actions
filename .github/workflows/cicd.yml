name: Test CICD
on:
  push:
    branches: 
      - main
    #paths-ignore:
    #  - '.github/workflows/*' 
  workflow_dispatch:

jobs:
  
  BUILD:
    runs-on: ubuntu-latest
    steps:
      - name: printing hello 
        run: echo "Starting the Building job"
      - name: Checkout Repository
        uses: actions/checkout@v4.1.1
      - name: Printling ls
        run: ls
      - name: Setup Go environment
        uses: actions/setup-go@v5.0.0
        with:
          go-version: 1.21
      - name: run test
        run: go test
      - name: Build the code
        run: go build -o compiled/app
      - name: print inside of the folder
        run: ls
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v4
        with:
          version: v1.54
          skip-cache: true
          skip-pkg-cache: true
          skip-build-cache: true
      - name: upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: app
          path: compiled

  DOWNLOAD_ARTIFACT:
    needs: BUILD
    runs-on: ubuntu-latest
    steps:
      - name: Download previous build artifact
        uses: actions/download-artifact@v4.1.4
        with:
          name: app
          path: compiled
      - name: Printing folder
        run: ls
        
          
  SECOND_TEST:
    needs: DOWNLOAD_ARTIFACT
    runs-on: ubuntu-latest
    steps:
      - name: printing the folder without checkout
        run: ls
      - name: Checkout Repository
        uses: actions/checkout@v4.1.1
      - name: printing the folder after checkout
        run: ls
      - name: running some other command
        run: |
          echo "starting the script"
          MY_VAR="Hello World"
          if [ MY_VAR=="Hello World" ]; then
            echo "VAR IS CORRECT SET"
            echo "value of VAR is $MY_VAR"
          else
            echo "VAR IS NOT SET CORRECTLY"
            echo "value of VAR is $MY_VAR"
          fi
    
